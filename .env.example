# ──────────────────────────────────────────────────────────────────
#  ZapDin PDV — Configuração
#  Renomeie para .env e preencha os valores.
# ──────────────────────────────────────────────────────────────────

# Evolution API rodando LOCALMENTE nesta máquina (porta padrão 8080)
# O WhatsApp conecta aqui — usa CPU/memória desta máquina, não do servidor
EVOLUTION_URL=http://localhost:8080
EVOLUTION_API_KEY=zapdin-pdv-local

# ZapDin App REMOTO — apenas para credenciais e configurações
# O PDV autentica aqui para buscar configs. Quem fala com Monitor é o App.
ZAPDIN_URL=https://app.seuservidor.com.br
ZAPDIN_USERNAME=usuario
ZAPDIN_PASSWORD=senha123

# Porta local do PDV (ERP chama http://localhost:4600)
PDV_PORT=4600

# Chave de segurança — ERP envia no header X-PDV-Key
PDV_API_KEY=minha-chave-pdv-123

# Nome identificador deste PDV (aparece no /status)
PDV_NOME=Caixa 01

# empresa_id para identificar as instâncias no Evolution API local
PDV_EMPRESA_ID=1

# Intervalo de re-autenticação no ZapDin App (minutos)
SESSION_REFRESH_MINUTES=60
